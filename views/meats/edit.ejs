<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meats Page</title>
</head>
<body>
    <h1>Edit Meats Page</h1>
    <form action="/meats/<%= id %>/?_method=PUT" method="POST">
        <label for="name">Name</label>
        <input type="text" name="name" id="name" value="<%= meat.name %>">
        <label for="color">Color</label>
        <input type="text" name="color" id="color" value="<%= meat.color %>">
        <label for="readyToEat">Is Ready to Eat</label>
        <input type="checkbox" name="readyToEat" id="readyToEat"
        
        <% if (meat.readyToEat === true) { %>
            checked
        <% } %>
        />
        <input type="submit" value="Create Meat">
    </form>
</body>
</html>